# Programs
alias less=$PAGER
alias zless=$PAGER
alias kd='source kd.sh'
alias g='git status'

# Filesystem nav
alias dave='cd ~/Development'
alias cdb='cd ..'

function mkdc {
  mkdir $1
  cd $1
}

alias rmd='rm -r'

# Utils
alias reload='source ~/.bashrc'
